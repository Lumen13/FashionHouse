@model FashionHouse.Data.ObjectModel.ProductModel

<h1>GET READY !</h1>

@*<table class="table table-striped table-dark">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Имя</th>
            <th scope="col">Кол-во</th>
            <th scope="col">Цена</th>
            <th scope="col">Категория</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <th scope="row">@Model.Id</th>
            <td><a href="/Product/@Model.Id" style="color: white">@Model.Name</a></td>
            <td><a href="/Product/@Model.Id" style="color: white">@Model.Count шт.</a></td>
            <td><a href="/Product/@Model.Id" style="color: white">@Model.Price руб.</a></td>
            <td><a href="/Product/@Model.Id" style="color: white">@Model.ProductCategory?.Name</a></td>
        </tr>
    </tbody>
</table>*@