@model FashionHouse.Data.ViewModel.ProductsView
<br />
<br />

<div>
    <img src="~/Images/main/filterBorder.png" alt="filterBolder" asp-append-version="true" class="align-self-start position-absolute" />
</div>

<div class="col-2 custom-control custom-radio my-blank">
    <label class="font-weight-bold">Категория:</label>
    @foreach (var model in Model.ProductModels)
    {
        @foreach (var category in model.ProductCategories)
        {
            if (category.IsParent == false)
            {
                <div>
                    <label class="my-paddingZero"><input class="align-self-start font-weight-normal" name="@category.Id" value="@category.Id" type="radio" /> @category.Name</label>
                </div>
            }
        }
        break;
    }
</div>
<div class="col-2"></div>
<div class="col-3"></div>
<div class="col-4"></div>
<div class="col-5"></div>
<div class="col-6"></div>